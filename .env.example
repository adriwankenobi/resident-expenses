# Absolute path to your config.json (kept outside the repo).
RESIDENT_EXPENSES_CONFIG=/absolute/path/to/config.json
# Shared password neighbors use to open the published report.
STATICRYPT_PASSWORD=change-me
